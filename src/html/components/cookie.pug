include ../elements/button

mixin cookie
  .cookie(data-cookie)
    .container
      .row
        .col-8.offset-2.d-flex.align-items-center
          .cookie__text
            |На&nbsp;сайте используются файлы cookie, чтобы предоставить больше возможностей использования. <a href="#" target="_blank">Узнать подробнее</a>.
        .col-2.d-flex.justify-content-between.align-items-center
          +button({
            name: 'Принять',
            style: 'secondary',
            tag: 'button',
						height: 'low'
          })&attributes({ type: 'button', 'data-cookie-close': '' })
          .cookie__close(data-cookie-close)
